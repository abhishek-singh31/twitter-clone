extends layouts/login-layout.pug

block content
    .loginContainer 
        h1 Twitter
        form(method='post')
            p.errMessage #{errorMessage}
            input(type="text", name="logUsername", placeholder='Enter Username', value=logUsername required="")
            input(type="password", name="logPassword", placeholder='Enter Password', required="")
       
            input(type="submit", value="Login")
        a(href="/register") Create New Account
